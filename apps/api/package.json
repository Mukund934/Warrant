{
  "name": "@warrant/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Express service that issues mandates, runs the authority gate and records evidence",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc --build --force",
    "clean": "tsc --build --clean",
    "start": "node dist/server.js",
    "dev": "node --watch dist/server.js"
  },
  "dependencies": {
    "@warrant/core": "0.1.0",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "supertest": "^7.2.2",
    "@types/supertest": "^6.0.3"
  }
}
