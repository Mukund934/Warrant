{
  "name": "@warrant/verifier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone offline verifier for Warrant evidence packs",
  "bin": {
    "warrant-verify": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build --force",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@warrant/core": "0.1.0"
  }
}
